#ifndef GLOBALS_HPP
#define GLOBALS_HPP

#include <iostream>

namespace GBL
{
	namespace DIR
	{
		const std::string base = "./wearerocking/";
		const std::string resources = base + "resources/";
		const std::string textures = resources + "textures/";
		const std::string fonts = resources + "fonts/";

		const std::string characters = base + "characters/";
	}
}

#endif // !GLOBALS_HPP
